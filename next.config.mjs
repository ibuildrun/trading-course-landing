/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/trading-course-landing',
  trailingSlash: true,
  images: { unoptimized: true },
};
export default nextConfig;
